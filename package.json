{
  "name": "firebase-authentication-poc",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "start": "lerna run start --parallel"
  },
  "devDependencies": {
    "lerna": "^8.1.3"
  },
  "resolutions": {
    "string-width": "4.2.3"
  }
}
